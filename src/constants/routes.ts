export enum AppRoutes {
  HOME = '/',
}

export enum AuthRoutes {
  SIGN_IN = '/auth/sign-in',
  SIGN_OUT = '/auth/sign-out',
}

export enum FotaRoutes {
  BIKES = '/fota/bike',
  ETBS = '/fota/etb',
  BATTERIES = '/fota/batery',
}

export enum VersionRoutes {
  CREATE_VERSION = '/version/create-version',
  LIST_VERSIONS = '/version/list-versions',
}
